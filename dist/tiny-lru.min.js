/*!
 2026 Jason Mulligan <jason.mulligan@avoidwork.com>
 @version 11.4.7
*/
class t {
	constructor(t = 0, s = 0, e = !1) {
		((this.first = null),
			(this.items = Object.create(null)),
			(this.last = null),
			(this.max = t),
			(this.resetTtl = e),
			(this.size = 0),
			(this.ttl = s));
	}
	clear() {
		return (
			(this.first = null),
			(this.items = Object.create(null)),
			(this.last = null),
			(this.size = 0),
			this
		);
	}
	delete(t) {
		const s = this.items[t];
		return (
			void 0 !== s &&
				(delete this.items[t],
				this.size--,
				null !== s.prev && (s.prev.next = s.next),
				null !== s.next && (s.next.prev = s.prev),
				this.first === s && (this.first = s.next),
				this.last === s && (this.last = s.prev),
				(s.prev = null),
				(s.next = null)),
			this
		);
	}
	entries(t) {
		void 0 === t && (t = this.keys());
		const s = Array.from({ length: t.length });
		for (let e = 0; e < t.length; e++) {
			const i = t[e],
				l = this.items[i];
			s[e] = [i, void 0 !== l ? l.value : void 0];
		}
		return s;
	}
	evict(t = !1) {
		if (t || this.size > 0) {
			const t = this.first;
			if (!t) return this;
			(delete this.items[t.key],
				0 == --this.size
					? ((this.first = null), (this.last = null))
					: ((this.first = t.next), (this.first.prev = null)),
				(t.next = null));
		}
		return this;
	}
	expiresAt(t) {
		const s = this.items[t];
		return void 0 !== s ? s.expiry : void 0;
	}
	get(t) {
		const s = this.items[t];
		if (void 0 !== s)
			return this.ttl > 0 && s.expiry <= Date.now()
				? void this.delete(t)
				: (this.moveToEnd(s), s.value);
	}
	has(t) {
		const s = this.items[t];
		return void 0 !== s && (0 === this.ttl || s.expiry > Date.now());
	}
	moveToEnd(t) {
		this.last !== t &&
			(null !== t.prev && (t.prev.next = t.next),
			null !== t.next && (t.next.prev = t.prev),
			this.first === t && (this.first = t.next),
			(t.prev = this.last),
			(t.next = null),
			(this.last.next = t),
			(this.last = t));
	}
	keys() {
		const t = Array.from({ length: this.size });
		let s = this.first,
			e = 0;
		for (; null !== s; ) ((t[e++] = s.key), (s = s.next));
		return t;
	}
	setWithEvicted(t, s, e = this.resetTtl) {
		let i = null,
			l = this.items[t];
		return (
			void 0 !== l
				? ((l.value = s),
					e && (l.expiry = this.ttl > 0 ? Date.now() + this.ttl : this.ttl),
					this.moveToEnd(l))
				: (this.max > 0 &&
						this.size === this.max &&
						((i = { key: this.first.key, value: this.first.value, expiry: this.first.expiry }),
						this.evict(!0)),
					(l = this.items[t] =
						{
							expiry: this.ttl > 0 ? Date.now() + this.ttl : this.ttl,
							key: t,
							prev: this.last,
							next: null,
							value: s,
						}),
					1 == ++this.size ? (this.first = l) : (this.last.next = l),
					(this.last = l)),
			i
		);
	}
	set(t, s, e = !1, i = this.resetTtl) {
		let l = this.items[t];
		return (
			e || void 0 !== l
				? ((l.value = s),
					!1 === e && i && (l.expiry = this.ttl > 0 ? Date.now() + this.ttl : this.ttl),
					this.moveToEnd(l))
				: (this.max > 0 && this.size === this.max && this.evict(!0),
					(l = this.items[t] =
						{
							expiry: this.ttl > 0 ? Date.now() + this.ttl : this.ttl,
							key: t,
							prev: this.last,
							next: null,
							value: s,
						}),
					1 == ++this.size ? (this.first = l) : (this.last.next = l),
					(this.last = l)),
			this
		);
	}
	values(t) {
		void 0 === t && (t = this.keys());
		const s = Array.from({ length: t.length });
		for (let e = 0; e < t.length; e++) {
			const i = this.items[t[e]];
			s[e] = void 0 !== i ? i.value : void 0;
		}
		return s;
	}
}
function s(s = 1e3, e = 0, i = !1) {
	if (isNaN(s) || s < 0) throw new TypeError("Invalid max value");
	if (isNaN(e) || e < 0) throw new TypeError("Invalid ttl value");
	if ("boolean" != typeof i) throw new TypeError("Invalid resetTtl value");
	return new t(s, e, i);
}
export { t as LRU, s as lru }; //# sourceMappingURL=tiny-lru.min.js.map
