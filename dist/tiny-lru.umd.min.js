/*!
 2026 Jason Mulligan <jason.mulligan@avoidwork.com>
 @version 11.4.7
*/
!(function (t, e) {
	"object" == typeof exports && "undefined" != typeof module
		? e(exports)
		: "function" == typeof define && define.amd
			? define(["exports"], e)
			: e(((t = "undefined" != typeof globalThis ? globalThis : t || self).lru = {}));
})(this, function (t) {
	"use strict";
	class e {
		constructor(t = 0, e = 0, i = !1) {
			((this.first = null),
				(this.items = Object.create(null)),
				(this.last = null),
				(this.max = t),
				(this.resetTtl = i),
				(this.size = 0),
				(this.ttl = e));
		}
		clear() {
			return (
				(this.first = null),
				(this.items = Object.create(null)),
				(this.last = null),
				(this.size = 0),
				this
			);
		}
		delete(t) {
			const e = this.items[t];
			return (
				void 0 !== e &&
					(delete this.items[t],
					this.size--,
					null !== e.prev && (e.prev.next = e.next),
					null !== e.next && (e.next.prev = e.prev),
					this.first === e && (this.first = e.next),
					this.last === e && (this.last = e.prev),
					(e.prev = null),
					(e.next = null)),
				this
			);
		}
		entries(t) {
			void 0 === t && (t = this.keys());
			const e = Array.from({ length: t.length });
			for (let i = 0; i < t.length; i++) {
				const s = t[i],
					l = this.items[s];
				e[i] = [s, void 0 !== l ? l.value : void 0];
			}
			return e;
		}
		evict(t = !1) {
			if (t || this.size > 0) {
				const t = this.first;
				if (!t) return this;
				(delete this.items[t.key],
					0 == --this.size
						? ((this.first = null), (this.last = null))
						: ((this.first = t.next), (this.first.prev = null)),
					(t.next = null));
			}
			return this;
		}
		expiresAt(t) {
			const e = this.items[t];
			return void 0 !== e ? e.expiry : void 0;
		}
		get(t) {
			const e = this.items[t];
			if (void 0 !== e)
				return this.ttl > 0 && e.expiry <= Date.now()
					? void this.delete(t)
					: (this.moveToEnd(e), e.value);
		}
		has(t) {
			const e = this.items[t];
			return void 0 !== e && (0 === this.ttl || e.expiry > Date.now());
		}
		moveToEnd(t) {
			this.last !== t &&
				(null !== t.prev && (t.prev.next = t.next),
				null !== t.next && (t.next.prev = t.prev),
				this.first === t && (this.first = t.next),
				(t.prev = this.last),
				(t.next = null),
				(this.last.next = t),
				(this.last = t));
		}
		keys() {
			const t = Array.from({ length: this.size });
			let e = this.first,
				i = 0;
			for (; null !== e; ) ((t[i++] = e.key), (e = e.next));
			return t;
		}
		setWithEvicted(t, e, i = this.resetTtl) {
			let s = null,
				l = this.items[t];
			return (
				void 0 !== l
					? ((l.value = e),
						i && (l.expiry = this.ttl > 0 ? Date.now() + this.ttl : this.ttl),
						this.moveToEnd(l))
					: (this.max > 0 &&
							this.size === this.max &&
							((s = { key: this.first.key, value: this.first.value, expiry: this.first.expiry }),
							this.evict(!0)),
						(l = this.items[t] =
							{
								expiry: this.ttl > 0 ? Date.now() + this.ttl : this.ttl,
								key: t,
								prev: this.last,
								next: null,
								value: e,
							}),
						1 == ++this.size ? (this.first = l) : (this.last.next = l),
						(this.last = l)),
				s
			);
		}
		set(t, e, i = !1, s = this.resetTtl) {
			let l = this.items[t];
			return (
				i || void 0 !== l
					? ((l.value = e),
						!1 === i && s && (l.expiry = this.ttl > 0 ? Date.now() + this.ttl : this.ttl),
						this.moveToEnd(l))
					: (this.max > 0 && this.size === this.max && this.evict(!0),
						(l = this.items[t] =
							{
								expiry: this.ttl > 0 ? Date.now() + this.ttl : this.ttl,
								key: t,
								prev: this.last,
								next: null,
								value: e,
							}),
						1 == ++this.size ? (this.first = l) : (this.last.next = l),
						(this.last = l)),
				this
			);
		}
		values(t) {
			void 0 === t && (t = this.keys());
			const e = Array.from({ length: t.length });
			for (let i = 0; i < t.length; i++) {
				const s = this.items[t[i]];
				e[i] = void 0 !== s ? s.value : void 0;
			}
			return e;
		}
	}
	((t.LRU = e),
		(t.lru = function (t = 1e3, i = 0, s = !1) {
			if (isNaN(t) || t < 0) throw new TypeError("Invalid max value");
			if (isNaN(i) || i < 0) throw new TypeError("Invalid ttl value");
			if ("boolean" != typeof s) throw new TypeError("Invalid resetTtl value");
			return new e(t, i, s);
		}));
}); //# sourceMappingURL=tiny-lru.umd.min.js.map
